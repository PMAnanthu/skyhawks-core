server:
  port: 8300
  servlet:
    context-path: /v0.1

spring:
  application:
    name: skyhawks-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PMAnanthu/skyhawks-config.git
          clone-on-start: true
