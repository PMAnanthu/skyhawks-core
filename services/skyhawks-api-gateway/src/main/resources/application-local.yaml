server:
  port: 8000
  servlet:
    context-path: /v0.1

spring:
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    url: jdbc:postgresql://localhost:5432/login
    username: ananthupm
    password: Password
  jpa:
    hibernate:
      ddl-auto: none

app:
  service:
    authenticate-user: true
    validate-user: true
    notification-service-url: http://localhost:9000/v0.1
    school-service-url: http://localhost:1000/v0.1


springdoc:
  swagger-ui:
    path: /swagger-ui-custom.html