server:
  port: 1000
  servlet:
    context-path: /v0.1

spring:
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    url: jdbc:postgresql://database-1.cc33tfhdinqi.ap-south-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: Aw5Pa$$wo6d
  jpa:
    hibernate:
      ddl-auto: none

app:
  service:
    authenticate-user: true
    validate-user: true
    gateway-service-url: http://gateway:8000/v0.1