server:
  port: 9000
  servlet:
    context-path: /v0.1

spring:
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  mail:
    host: smtp.gmail.com
    port: 587
    username: skyhawks.app@gmail.com
    password: nuvldcdnuafzcbxv
    properties:
      mail:
        debug: true
        transport:
          protocol: smtp
        smtp:
          auth: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000
          starttls:
            enable: true

springdoc:
  swagger-ui:
    path: /v0.1/swagger-ui-custom.html